# Production: direct Railway backend URL (CORS enabled on backend).
# Set VITE_API_BASE_URL in Vercel Environment Variables if this file isn't used.
VITE_API_BASE_URL=https://rssint-production.up.railway.app
